function mostrar()
{
	//tomo la edad  txtIdEdad

	var edad;

	edad=txtIdEdad.value;

	edad=parseInt(edad);
	if (edad == 15);
	{ 
		alert("niña bonita");
	}

}
/* 
operadores aritmético: +, -, *, /, %.
relacionales: >, <, >=, <=, !=, ==.
lógicos: &&(and), || (or), ! (no). */